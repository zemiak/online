connect

###
# Remove Database and Mail Connection
###

if (outcome == success) of /subsystem=datasources/data-source=ZemiakMovies/:read-resource
    /subsystem=datasources/data-source=ZemiakMovies/:disable
    reload
    /subsystem=datasources/data-source=ZemiakMovies/:remove
end-if

if (outcome == success) of /subsystem=mail/mail-session=MoviesMail/:read-attribute(name=jndi-name)
    /subsystem=mail/mail-session=MoviesMail/:remove
    reload
end-if

reload
